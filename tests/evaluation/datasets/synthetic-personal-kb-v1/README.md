# synthetic-personal-kb-v1

**Status: FROZEN / historical**

The V2.0 evaluation assets (6 documents, 31 pages, 30 retrieval queries,
12 answer cases) and their generators/validators live here byte-for-byte as
committed at the V2.0 milestone.

- Corpus / PDFs / fonts / anchors: `fixtures/`
- Queries & answer cases: `dataset/`
- Baseline report: `../reports/baseline-v2.0.json` (never re-run)

No active code defaults to this dataset. It is referenced explicitly only for
framework regression and historical comparison.
