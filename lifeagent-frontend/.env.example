# LifeAgent Frontend V1
#
# 开发环境使用相对路径：浏览器只访问 Vite dev server（5173），
# 由 vite.config.ts 把 /api/v1 代理到 FastAPI（宿主机 8080）。
# 这样浏览器侧不存在跨域，后端无需为此添加 CORS。
#
# 部署时再根据架构决定：Nginx 同源转发 /api，或直连 https://api.xxx.com + CORS。
VITE_API_BASE_URL=/api/v1
